# the database file is too large for github, but can easily be recreated
*.db

# fasta reference genome is ~3GB, don't want to endlessly back that up!
hg19/*.fa
*.fa.fai

# various intermediate files are created by scripts
*.allexons*
*.nosplicesites*

# hg19 folder contains links, not files, but just to be safe
hg19/

# Try to ignore temp files from gedit
*.*~

# input files can be quite large, and are generated from the database anyway.
# but literature variants are ok
input.refseq*
input.random*

# sql upload files are pretty big, and can generate themselves
*upload.sql

# same for any generated vcfs
*.vcf
